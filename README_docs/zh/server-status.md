# 📡 服务器状态

实时展示 Minecraft 服务器的在线状态、玩家数、版本等信息。

## 特性

- 实时监控：定期轮询服务器状态接口（`/systemstatus/data`）。
- 状态页：独立 `/status` 与 `/status/mcserverstatus` 页面，便于分享与嵌入。
- 异常兜底：接口不可用时展示降级状态，不阻塞页面渲染。

## 接入说明

状态数据通过服务端接口聚合后下发，避免浏览器直接暴露查询源，兼具稳定性与隐私保护。
